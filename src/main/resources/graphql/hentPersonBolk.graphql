query($identer: [ID!]!) {
    hentPersonBolk(identer: $identer) {
        ident,
        person {
            navn {
                fornavn,
                mellomnavn,
                etternavn,
            }
            adressebeskyttelse {
                gradering
            }
        },
        code
    }
}